exports.add = (x, y) => x + y
exports.sub = (x, y) => x - y
exports.mul = (x, y) => x * y
exports.div = (x, y) => x / y
exports.mod = (x, y) => x % y

//CW3: WAF addsub(x, y) to return addition 
//and subtraction in array

exports.addsub = (x, y) => [x + y, x - y]

//CW3: WAF muldiv(x, y) to return multiplication 
//and division in array









/*
CW2:
create 4 function and export
1 sub 
2 mul 
3 div
4 mod / remainder
*/



/*
function add(x, y) {
    return x + y
}

//convert add funcition to variable function
add = function(x, y) {
    return x + y
}

//convert add function to lambda function
add = (x, y) => {
    return x + y
}

//convert add funtion to single line function by removing return statement
add = (x, y) => x + y

console.log(add(5, 6))
*/
